# 🚀 Автоматическая настройка Supabase

## Проблема
Прямое подключение к Supabase PostgreSQL не работает из-за неверных credentials.

## Решение: Через Supabase Dashboard

### Шаг 1: Создай схему БД

1. Открой https://supabase.com/dashboard/project/nywsibcnngcexjbotsaq/sql/new
2. Скопируй содержимое файла `new_schema.sql`
3. Вставь в SQL Editor
4. Нажми **RUN** (или Ctrl+Enter)
5. Дождись выполнения (~5-10 секунд)

### Шаг 2: Мигрируй данные

После создания схемы запусти:

```bash
node migrate-to-supabase.js
```

Этот скрипт:
- Экспортирует ВСЕ данные из локальной БД
- Загрузит их в Supabase через REST API
- Сохранит копию в `/tmp/local_db_export.json` (на всякий случай)

### Шаг 3: Переключи .env.local

Обнови `DATABASE_URL` в `.env.local`:

```env
# Используй Session mode pooler
DATABASE_URL=postgresql://postgres.nywsibcnngcexjbotsaq:[ТВОЙ_ПАРОЛЬ]@aws-0-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true
```

**Где взять пароль?**
- Dashboard → Settings → Database → "Reset database password"
- ИЛИ используй тот что уже есть (если сохранил)

### Шаг 4: Перезапусти

```bash
pkill -f "next dev"
npm run dev
```

---

## ✅ Результат

После этого:
- Все логины, воронки, сделки будут в Supabase
- Локальная БД больше не нужна
- Сайт работает быстро и стабильно

---

## 📊 Экспортированные данные

Сейчас в локальной БД:
- Accounts: 3
- Users: 2  
- Pipelines: 3
- Stages: 15
- Companies: 5
- Contacts: 8
- Deals: 20
- Deal Contacts: 18

Все это будет перенесено в Supabase!

---

## 💡 Почему не автоматически?

Supabase блокирует прямые подключения с неверными credentials. Нужно:
1. Либо сбросить пароль в Dashboard
2. Либо использовать SQL Editor (это быстрее)

Я создал все скрипты, осталось только выполнить 3 шага выше! 🎯
